[project]
name = "foodsight"
version = "0.1.0"
description = "FoodSight Streamlit app."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "streamlit>=1.58.0",
    "Pillow>=10.0",
]

[tool.uv]
package = false
