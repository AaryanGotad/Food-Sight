[project]
name = "blank-app-template"
version = "0.1.0"
description = "A simple Streamlit app template."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "streamlit>=1.58.0",
    "pandas>=2.0",
    "numpy>=1.26",
]

[tool.uv]
package = false
